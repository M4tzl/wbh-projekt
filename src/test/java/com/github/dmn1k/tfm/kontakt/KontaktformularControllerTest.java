package com.github.dmn1k.tfm.kontakt;

import org.junit.Test;

import static org.junit.Assert.*;

public class KontaktformularControllerTest {

    @Test
    public void send() {
        try {
            Thread.sleep((long) (Math.random() * 500));
        } catch (InterruptedException e) {

        }
    }
}
